before run the app_john.py, don't forget double check all the path.
and CSS file is only for the apperance part

(Every time the code is modified, it needs to be run again. The reason for not using reload is that there are some unknown errors that will cause the web page to jump automatically.)
run in terminal:
python -m shiny run app_john.py
